import 'package:flutter/material.dart';

class ProfileTest extends StatelessWidget {
  const ProfileTest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("hello profiles", style: TextStyle(color: Colors.white)),
      ),
    );
  }
}
