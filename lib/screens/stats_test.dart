import 'package:flutter/material.dart';

class StatsTest extends StatelessWidget {
  const StatsTest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("hello stats", style: TextStyle(color: Colors.white)),
      ),
    );
  }
}
